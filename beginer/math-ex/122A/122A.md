Chúng ta sẽ cùng nhau "mổ xẻ" bài **122A - Lucky Division** trên Codeforces theo phong cách tư duy thuật toán nhé. Tôi sẽ đồng hành cùng bạn để biến bài toán này thành những mảnh ghép logic dễ hiểu. 🧠

### 🔍 Bước 1: Phẫu thuật đề bài (Briefing)

Loại bỏ câu chuyện về chú gấu Petya, đây là những gì cốt lõi nhất chúng ta cần xử lý:

1. **Định nghĩa "Số may mắn" (Lucky Number):** Là những số chỉ chứa các chữ số  và . Ví dụ: .
2. **Định nghĩa "Số suýt may mắn" (Almost Lucky Number):** Một số  được gọi là "suýt may mắn" nếu nó **chia hết** cho bất kỳ một "số may mắn" nào đó.
3. **Nhiệm vụ:** Nhập vào một số  (). Kiểm tra xem  có phải là "số suýt may mắn" hay không. Nếu có in `YES`, ngược lại in `NO`.

**Lộ trình tư duy của chúng ta:**

* **Chunk 1:** Nhận diện "Số may mắn" trong phạm vi cho phép.
* **Chunk 2:** Kiểm tra tính chia hết (Chiến thuật "Bắn hạ mục tiêu").
* **Chunk 3:** Tổng kết và chốt phương án.

Bạn đã sẵn sàng bước vào **Chunk 1** để tìm xem có bao nhiêu "vị thần may mắn" có thể xuất hiện chưa? 🚀

Tuyệt vời! Chúng ta sẽ bắt đầu với **Chunk 1: Nhận diện "Số may mắn"**. 🍀

Vì đề bài giới hạn số  trong khoảng từ  đến , nên các "số may mắn" (chỉ gồm chữ số  và ) mà chúng ta cần quan tâm cũng chỉ nằm trong khoảng này thôi.

Bây giờ, hãy thử liệt kê nhanh ra nháp các số may mắn nhỏ hơn hoặc bằng  nhé. Chúng ta có thể chia theo số lượng chữ số:

* **Có 1 chữ số:** 
* **Có 2 chữ số:** 
* **Có 3 chữ số:**  (và các tổ hợp khác của  và ).

### ❓ Thử thách tư duy cho bạn:

Trong các số sau đây, số nào **KHÔNG PHẢI** là "số may mắn"?
A. 
B. 
C. 
D. 

Hãy chọn đáp án và giải thích ngắn gọn tại sao nhé! Sau đó chúng ta sẽ xem làm cách nào để máy tính kiểm tra tính "suýt may mắn" một cách nhanh nhất.

Chính xác! 🎯 Con số  đã "tố cáo"  không phải là số may mắn, vì số may mắn chỉ được phép chứa  và .

Bây giờ chúng ta bước sang **Chunk 2: Chiến thuật "Bắn hạ mục tiêu"**.

Để kiểm tra xem  có phải là "số suýt may mắn" hay không, ta cần xem  có chia hết cho **bất kỳ** số may mắn nào không. Vì , chúng ta chỉ cần lập một "danh sách truy nã" các số may mắn nhỏ hơn  và cho  lần lượt "so kèo" với từng số đó.

Dưới đây là danh sách các số may mắn chúng ta cần:
`4, 7, 44, 47, 74, 77, 444, 447, 474, 477, 744, 747, 774, 777`

> **Cạm bẫy (Trap):** Nhiều bạn thường quên mất rằng chỉ cần chia hết cho **MỘT** số trong danh sách trên là đủ để kết luận `YES`. Không cần phải chia hết cho tất cả đâu nhé!

### ❓ Thử thách tư duy:

Giả sử người dùng nhập vào số ****.

Dựa vào danh sách số may mắn ở trên, bạn hãy thử làm "máy tính" và kiểm tra xem:

1.  có chia hết cho số nào trong danh sách không?
2. Kết quả cuối cùng bài này nên in ra là `YES` hay `NO`?

Giải thích giúp mình tại sao bạn chọn kết quả đó nhé! 🧐

Chuẩn không cần chỉnh! 🎯 Số  chia hết cho  (một số may mắn), nên theo định nghĩa của đề bài,  chính là một "số suýt may mắn". Kết quả in ra sẽ là `YES`.

Bây giờ, chúng ta sẽ bước vào mảnh ghép cuối cùng để chốt hạ bài toán này: **Chunk 3: Tổng kết và chốt thuật toán**.

Để giải quyết bài này một cách gọn gàng nhất, chúng ta sẽ sử dụng chiến thuật **"Duyệt danh sách"**. Vì phạm vi  nhỏ (), ta không cần dùng những thuật toán tạo số phức tạp.

**Quy trình thực hiện (Mã giả):**

1. **Tạo một danh sách** chứa tất cả các số may mắn từ  đến  (như chúng ta đã liệt kê ở trên). 📝
2. **Nhập số ** từ bàn phím.
3. **Dùng một vòng lặp** đi qua từng số trong danh sách đó:
* Nếu  chia hết cho số đang xét (dùng phép chia lấy dư `% == 0`).
* Thì lập tức đánh dấu là "Tìm thấy" và dừng lại.


4. **Kiểm tra kết quả:** Nếu "Tìm thấy" thì in `YES`, ngược lại in `NO`.

### ❓ Thử thách cuối cùng trước khi bạn tự viết code:

Nếu mình nhập vào số ****. Bạn hãy nhìn lại danh sách số may mắn này một lần nữa:
`4, 7, 44, 47, 74, 77, 444, 447, 474, 477, 744, 747, 774, 777`

Theo bạn, với , máy tính sẽ in ra `YES` hay `NO`? Và tại sao? (Gợi ý: Hãy thử chia  cho vài số đầu tiên trong danh sách xem có dư không nhé). 🧐

Bạn trả lời rất dứt khoát! Đúng vậy, số **** không chia hết cho bất kỳ số may mắn nào trong danh sách (nó không chia hết cho , cũng không chia hết cho , và hiển nhiên là không chia hết cho các số lớn hơn). Vì vậy, kết quả cho  phải là `NO`. ❌

Chúng ta đã đi qua toàn bộ các mảnh ghép tư duy: từ việc hiểu thế nào là số may mắn, đến việc lập danh sách và cuối cùng là kiểm tra tính chia hết.

Bây giờ là lúc để bạn tổng hợp lại tất cả thành một cấu trúc logic hoàn chỉnh. Đây là bước **"Chốt thuật toán"** bằng mã giả (Pseudocode) đơn giản:

---

### 📝 Mã giả (Tư duy lập trình)

```text
BƯỚC 1: Tạo một mảng (danh sách) chứa các số: 
        4, 7, 44, 47, 74, 77, 444, 447, 474, 477, 744, 747, 774, 777

BƯỚC 2: Đọc số nguyên n từ bàn phím.

BƯỚC 3: Tạo một biến "kiem_tra" và đặt là False (nghĩa là chưa tìm thấy).

BƯỚC 4: Chạy vòng lặp qua từng số "x" trong danh sách ở Bước 1:
        Nếu n chia cho x dư 0:
           Đặt "kiem_tra" thành True.
           Dừng vòng lặp (vì tìm thấy 1 số là đủ rồi).

BƯỚC 5: Nếu "kiem_tra" là True:
           In ra "YES".
        Ngược lại:
           In ra "NO".

```

---

**Câu hỏi cuối cùng dành cho bạn:**

Trong bước 4, tại sao chúng ta nên dùng lệnh "Dừng vòng lặp" (trong code thường là `break`) ngay khi tìm thấy một số mà  chia hết?

1. Để tiết kiệm thời gian chạy của máy tính. ⚡
2. Vì nếu không dừng, máy tính sẽ bị lỗi. ⚠️
3. Vì đề bài yêu cầu chỉ được chia hết cho đúng một số duy nhất. ☝️

Bạn chọn phương án nào? Sau khi trả lời xong, bạn có muốn mình hỗ trợ viết thử đoạn code C++ hoặc Python mẫu cho bài này không? 💻