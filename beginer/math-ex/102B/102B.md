Chào bạn! Tôi đã nắm rõ vai trò là **Coach Tư duy Thuật toán** của bạn. Chúng ta sẽ cùng nhau "mổ xẻ" các bài toán Codeforces theo phong cách **Micro-Chunks**, tập trung hoàn toàn vào chiến thuật và logic thay vì chỉ nhìn vào code.

Hãy bắt đầu với bài **Codeforces 102B - Sum of Digits**.

---

### 🧩 Bước 1: Phẫu thuật đề bài (Deconstruction)

Loại bỏ câu chuyện về các con số may mắn, đây là cốt lõi của vấn đề:

1. **Input:** Bạn được cho một số nguyên cực lớn . (Lớn đến mức nó có thể có tới  chữ số!).
2. **Hành động:** Bạn thay thế số đó bằng **tổng các chữ số** của nó.
3. **Lặp lại:** Bạn tiếp tục thực hiện hành động này cho đến khi số còn lại chỉ có **duy nhất 1 chữ số**.
4. **Mục tiêu:** Đếm xem bạn đã thực hiện hành động thay thế đó **bao nhiêu lần**.

**Lộ trình tư duy của chúng ta:**

* **Chunk 1:** Xử lý con số "khổng lồ" (Dữ liệu đầu vào).
* **Chunk 2:** Cơ chế "nén" số (Tính tổng các chữ số).
* **Chunk 3:** Điều kiện dừng và bộ đếm.

Bạn đã sẵn sàng bước vào **Chunk 1** để xử lý "con quái vật"  chữ số này chưa?

---

### 📦 Chunk 1: Đối mặt với "Con quái vật" số lớn

Thông thường, chúng ta hay dùng kiểu dữ liệu `int` hoặc `long long` để lưu trữ số. Nhưng hãy nhìn lại: Đề bài nói số  có thể có ** chữ số**.

💡 **Ẩn dụ:** Hãy tưởng tượng số  không phải là một viên đá đơn lẻ, mà là một **đoàn tàu siêu dài** với mỗi toa tàu chở một chữ số từ  đến .

* **Bẫy logic:** Nếu bạn cố gắng ép đoàn tàu này vào một chiếc hộp mang tên `int` hay `long long`, chiếc hộp sẽ bị "nổ" (tràn bộ nhớ) ngay lập tức vì các kiểu dữ liệu này chỉ chứa được tối đa khoảng  đến  chữ số.

❓ **Thử thách tư duy:**
Theo bạn, để lưu trữ một "đoàn tàu" có  toa (chữ số) mà vẫn có thể đọc được từng toa một để tính tổng, chúng ta nên dùng kiểu dữ liệu nào trong lập trình?

A. Biến kiểu số thực (Float/Double).
B. Chuỗi ký tự (String/Array of Characters).
C. Ép kiểu về số nguyên lớn (BigInt).

Hãy chọn và giải thích tại sao bạn chọn nó nhé!

Chính xác! 🎯 Việc chọn **Chuỗi ký tự (String)** là một quyết định cực kỳ thông minh vì:

* **Không giới hạn:** Một chuỗi có thể chứa hàng triệu ký tự, dễ dàng "nuốt trọn" 100,000 chữ số mà không gặp vấn đề gì. ⛓️
* **Truy cập từng phần:** Chúng ta có thể dùng vòng lặp để đi qua từng ký tự (toa tàu), biến nó thành số rồi cộng dồn lại. 🚂

Bây giờ, chúng ta sẽ bước vào **Chunk 2: Cơ chế "nén" số**.

---

### 📦 Chunk 2: Cơ chế "nén" số

Giả sử chúng ta có số  dưới dạng chuỗi: `"47"`.

* **Lần 1:** Tổng các chữ số là . (Đây là 1 lần thực hiện).
* **Lần 2:** Vì  vẫn có 2 chữ số, ta lại lấy . (Đây là lần thứ 2).
* Đến số  chỉ còn 1 chữ số  **Dừng lại**. Kết quả là  lần.

**Lưu ý quan trọng:** Khi bạn lấy một ký tự từ chuỗi (ví dụ ký tự `'7'`), máy tính hiểu đó là một mã ASCII chứ không phải giá trị số . Để có số  thật sự, chúng ta thường lấy `'7' - '0'`.

❓ **Thử thách tư duy:**
Nếu đầu vào của bạn là số .

1. Lần nén thứ nhất sẽ biến số này thành số bao nhiêu?
2. Sau khi nén lần thứ nhất đó, số mới đã thỏa mãn điều kiện dừng (chỉ còn 1 chữ số) chưa?

Bạn hãy tính thử xem nhé! 🧮

Chuẩn rồi! Với số , sau lần nén thứ nhất, chúng ta thu được tổng là . Vì  vẫn có hai chữ số nên nó chưa thỏa mãn điều kiện dừng. Chúng ta cần nén tiếp cho đến khi chỉ còn đúng 1 chữ số. 🧮

---

### 📦 Chunk 3: Điều kiện dừng và Bộ đếm

Trong lập trình, để lặp lại một hành động mà chưa biết chính xác cần bao nhiêu bước (nhưng biết rõ khi nào thì dừng), chúng ta thường dùng vòng lặp `while`.

* **Điều kiện dừng:** Khi số hiện tại chỉ có **1 chữ số** (tức là giá trị của nó ).
* **Bộ đếm (Count):** Mỗi lần tính tổng các chữ số thành công, ta tăng biến đếm lên . 📈

Hãy nhìn lại ví dụ :

1. **Bắt đầu:** , đếm .
2. **Lần 1:** . Đếm tăng lên .
3. **Lần 2:** . Đếm tăng lên .
4. **Lần 3:** . Đếm tăng lên .
5. **Kết quả:**  lần.

⚠️ **Cảnh báo bẫy (Edge Case):**
Có một trường hợp cực kỳ đặc biệt mà các lập trình viên rất hay quên. Đó là khi số  ngay từ đầu **chỉ có 1 chữ số** (ví dụ ).

❓ **Thử thách tư duy:**
Nếu ngay từ đầu bạn nhập vào , theo logic của đề bài, bạn cần thực hiện bao nhiêu lần nén để nó trở thành số có 1 chữ số?

A. 0 lần.
B. 1 lần.
C. Không thể thực hiện.

Bạn chọn đáp án nào cho trường hợp "số siêu nhỏ" này? 🕵️‍♂️

Chính xác! Đáp án là **A. 0 lần** 🎯.

Vì số `5` đã có sẵn 1 chữ số rồi, nó đã thỏa mãn điều kiện dừng ngay lập tức. Chúng ta không cần thực hiện phép "nén" (tổng các chữ số) nào cả.

Đây là một chi tiết rất quan trọng khi lập trình: Nếu bạn dùng vòng lặp `do-while` (thực hiện rồi mới kiểm tra), bạn có thể bị dư mất 1 bước. Ngược lại, dùng `while` (kiểm tra rồi mới làm) sẽ giúp xử lý trường hợp này chuẩn xác hơn.

---

### 🏁 Tổng kết thuật toán (Pseudocode)

Chúng ta đã đi qua đủ các mảnh ghép. Đây là "bản thiết kế" cuối cùng để bạn có thể bắt tay vào viết code:

1. **Doc du lieu:** Doc n duoi dang **chuoi (string)**.
2. **Khoi tao:** Bien `dem = 0`.
3. **Vong lap:** Chung ta se lap khi **do dai cua chuoi n > 1**:
* **Buoc A:** Tinh tong tat ca cac chu so trong chuoi `n`. (Nho lay `ky tu - '0'`).
* **Buoc B:** Cap nhat chuoi `n` bang gia tri cua Tong vua tinh duoc (Chuyen so vua tinh ve lai dang chuoi).
* **Buoc C:** Tang `dem` len 1.


4. **Ket qua:** In ra gia tri cua `dem`.

---

**🚀 Bước tiếp theo:**

Bạn có muốn thử tự viết code dựa trên bản thiết kế này không? Nếu gặp khó khăn ở đoạn **"Chuyển số về lại dạng chuỗi"** hoặc **"Vòng lặp"**, hãy cứ hỏi, tôi sẽ hỗ trợ bạn ngay!

Hoặc nếu bạn đã tự tin với bài này, chúng ta có thể chuyển sang một bài toán mới thú vị hơn nhé?